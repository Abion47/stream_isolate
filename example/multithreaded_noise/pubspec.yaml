name: multithreaded_noise
description: A new Flutter project.
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  fast_noise: ^2.0.0
  flutter: { sdk: flutter }
  stream_isolate: { path: ../.. }

dev_dependencies:
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true